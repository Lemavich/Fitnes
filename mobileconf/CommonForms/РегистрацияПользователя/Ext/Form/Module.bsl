
&НаКлиенте
Процедура Войти(Команда)
    Если ЭтаФорма.Логин = "" Тогда
        Сообщить("Введите логин пользователя!");
        Возврат;
	КонецЕсли;
	Если ЭтаФорма.Телефон = "" Тогда
        Сообщить("Введите телефон пользователя!");
        Возврат;
	КонецЕсли;
	
	
    АутентификацияИАвторизация.ЗарегистрироватьПользователя(ЭтаФорма.Логин, ЭтаФорма.Телефон);
    ЗавершитьРаботуСистемы(Ложь, Истина);

КонецПроцедуры
